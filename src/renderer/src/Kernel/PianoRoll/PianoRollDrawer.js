import CanvasController from "../../Core/Canvas/canvas_controller";

class PianoRollDrawer extends CanvasController {

  constructor(region, canvas) {
    super();
    this.region = region;
  }

  
}

export default PianoRollDrawer;